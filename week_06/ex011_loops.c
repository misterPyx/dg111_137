#include <stdio.h>
#include <math.h>

int main() // for loop
{

    for (int i = 1; i <= 10; i++)
    {
        printf("%d ", i);
    }

    printf("summary : %d", (10 * (10 + 1)) / 2);

    return 0;
}
